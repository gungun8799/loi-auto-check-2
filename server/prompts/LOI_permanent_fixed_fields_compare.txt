You are given extracted data from  two sources: PDF, Web,. These contain structured information from rent contracts.

Your task is to:
1. Extract the following fields from each source (if available):
 "อยู่กองทรัสต์หรือไม่" (if this field is "Yes" always mark as match),
"Building Name" (this field will contain word "ซีพี" if the field "อยู่กองทรัส" has "Yes" value, just validate as valid),
  "Lease Type" (Need to check "4. ค่าเช่าและบริการ" whether the payment condition is "Permanent Fixed" or not ),
  "Brand Name" (ชื่อร้าน or brand name),
  "Customer Name",
  "Customer Address" if the contract number contain "LR" no need to compare this field: just mark as Match,
  "Tenant Type",(This field is typically found near the Unit Number, which often begins with “PN”. Rather than relying on exact string patterns, infer the Tenant Type based on the surrounding context or label. For web scrape, extract the descriptive portion after the dash (”-”) that indicates the nature of the business or tenant — e.g., from “PN7 - PN Furniture”, extract “Furniture”, even if the prefix format varies),
  "Proposed lease commencement date",
  "Proposed lease expiry date",
  "Unit ID", (in this format PXXXX where x is the number )
  "Space (NLA)", (Area in square meter),
  "Billing Frequency" if the contract number contain "LR" mark as Match if the Web info is NULL, if not NULL then compare as usual,
  "Monthly charge", (Output in "Yes", or "No"),
  
"Year 1 : Contract Start date",  
• Interpret the PDF and web values as calendar dates and compare by actual date. If both means the same day, set match:true; otherwise match:false.

"Year 1 : Contract End date", 
• Interpret the PDF and web values as calendar dates and compare by actual date. If both means the same day, set match:true; otherwise match:false.

"Year 1 : Charge Type",  
• Compare the meaning of the PDF and web values: if both indicate a fixed rate, or both indicate a variable rate, set match:true; otherwise match:false.

"Year 1 : Monthly Amount of rent",  
• Compare the underlying numeric value in PDF vs. web. If they represent the same amount, set match:true; otherwise match:false.

"Year 1 : Monthly Amount of service",  
• Compare the underlying numeric value in PDF vs. web. If they represent the same amount, set match:true; otherwise match:false.

"Year 2 : Contract Start date",  
• Interpret the PDF and web values as calendar dates and compare by actual date. If both fall on the same day, set match:true; otherwise match:false.

"Year 2 : Contract End date",  
• Interpret the PDF and web values as calendar dates and compare by actual date. If both fall on the same day, set match:true; otherwise match:false.

"Year 2 : Charge Type",  
• Compare the meaning of the PDF and web values: if both indicate a fixed rate, or both indicate a variable rate, set match:true; otherwise match:false.

"Year 2 : Monthly Amount of rent",  
• Compare the underlying numeric value in PDF vs. web. If they represent the same amount, set match:true; otherwise match:false.

"Year 2 : Monthly Amount of service",  
• Compare the underlying numeric value in PDF vs. web. If they represent the same amount, set match:true; otherwise match:false.

"Year 3 : Contract Start date",  
• Interpret the PDF and web values as calendar dates and compare by actual date. If both fall on the same day, set match:true; otherwise match:false.

"Year 3 : Contract End date",  
• Interpret the PDF and web values as calendar dates and compare by actual date. If both fall on the same day, set match:true; otherwise match:false.

"Year 3 : Charge Type",  
• Compare the meaning of the PDF and web values: if both indicate a fixed rate, or both indicate a variable rate, set match:true; otherwise match:false.

"Year 3 : Monthly Amount of rent",  
• Compare the underlying numeric value in PDF vs. web. If they represent the same amount, set match:true; otherwise match:false.

"Year 3 : Monthly Amount of service",  
• Compare the underlying numeric value in PDF vs. web. If they represent the same amount, set match:true; otherwise match:false.



  "Other service charge (in the renting space)",
  "Other service charge (in the renting space) Charge description", often this will be "ค่าบริการพิเศษสำหรับไอเย็นและไฟฟ้า" which is the same meaning as "Common Area Cool Air Service Fee" just mark as match,
(if "Other service charge (in the renting space)" is 0, this field valid of null)
  "Other service charge (in the renting space) start date", if the Other service charge (in the renting space) is not null, just put the start date as the start date of the contract.
  "Other service charge (in the renting space) end date", if the Other service charge (in the renting space) is not null, just put the end date as the end date of the contract.
  "Other service charge (Common area)",
  "Other service charge (Common area) Charge description", often this will be "ค่าบริการสำหรับไอเย็น" which is the same meaning as "Common Area Cool Air Service Fee". match them by context,if they have the same meaning, just mark as match,
(if "Other service charge (in the renting space)" is 0, this field valid of null)
  "Other service charge (Common area) start date", if the Other service charge (Common area) is not null, just put the start date as the start date of the contract.
  "Other service charge (Common area) end date", if the Other service charge (Common area) is not null, just put the start date as the start date of the contract.

  "Include Utility", (if the field "อยู่กองทรัสต์หรือไม่" is "Yes", just mark this field as Match regardless of the matching string)
  "Utilities charge (water) "if the field "อยู่กองทรัสต์หรือไม่" is "Yes", just mark this field as match, 
  "Utilities charge (Electricity)" if the field "อยู่กองทรัสต์หรือไม่" is "Yes", just mark this field as match ,
  "Utilities charge (cooking gas)" if the field "อยู่กองทรัสต์หรือไม่" is "Yes", just mark this field as match, 

  "Total Rent Deposits",
  "Total Service Deposits",
  "Lease property tax", 
  When extracting the “Lease property tax” field, apply these rules to decide if the PDF value is valid compared to the web value:
	1.	Web value: “PTS-PT Standard” ignore the space just evaluate the similarity
	•	PDF value must be exactly “10.00” to be valid.
	2.	Web value: “PT Mall”  ignore the space just evaluate the similarity
	•	PDF value must be exactly “12.5” to be valid.
	3.	Web value: null or missing
	•	PDF value must be “0” to be valid.


2. Compare each field across the sources.

3. Allow for flexible comparison — match values that are contextually similar, even if formatted differently. Examples:
   - "March 10, 2025" vs "10/03/2025"
   - "$1,000.00" vs "1000"
   - "ซีพี" vs "CP"
   - With/without commas, units like "THB", or different date formats

4. If a value is missing in one or more sources, mark the match as **false**.

5. Return the result as a list of objects in this JSON format:

```json
[
  {
    "field": "Customer",
    "pdf": "Wisarut Gunjarueg",
    "web": "Mr. Wisarut G.",
    "match": true,
    "reason": "All refer to the same person with different naming styles"
  },
  {
    "field": "Rent charge",
    "pdf": "$15,000.00",
    "web": "15000",
    "match": true,
    "reason": "Same value represented in different formats"
  },
  {
    "field": "Contract Start Date",
    "pdf": "March 10, 2025",
    "web": "10/03/2025",
    "match": false,
    "reason": "Missing value in Excel"
  }
]